# name: deploy my blog
# on:
#   push:
#     branches:
#       - develop
# jobs:
#   build:
#     runs-on: ubuntu-18.04
#     if: github.event.repository.owner.id == github.event.sender.id

#     steps:
#       - name: Checkout source 🛎️
#         uses: actions/checkout@master

#       - name: Setup Node.js
#         uses: actions/setup-node@v1
#         with:
#           node-version: "12"

#       - name: Install and Build 🔧
#         run: |
#           npm install
#           npm run build

#       - name: Deploy 🚀
#         uses: JamesIves/github-pages-deploy-action@4.1.1
#         with:
#           branch: dev-pages # The branch the action should deploy to.
#           folder: public # The folder the action should deploy.
#           ssh-key: ${{ secrets.BLOG_PRIVATE_KEY }}

# - name: setup hexo
#   env:
#     BLOG_PRIVATE_KEY: ${{secrets.BLOG_PRIVATE_KEY}}
#     BRANCH: dev-pages

name: Build and Deploy
on:
  push:
    branches:
      - "develop"
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1.1.0
        with:
          submodules: recursive
      - name: Build and Deploy
        uses: ApolloZhu/hexo-deploy-action@master
        env:
          PERSONAL_TOKEN: ${{ secrets.BLOG_PRIVATE_KEY }}
          PUBLISH_REPOSITORY: qiongxing/qiongxing.github.io # The repository the action should deploy to.
          BRANCH: master # The branch the action should deploy to.
          PUBLISH_DIR: ./public # The folder the action should deploy.

