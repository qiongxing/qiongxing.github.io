# name: deploy my blog
# on:
#   push:
#     branches:
#       - develop
# jobs:
#   build:
#     runs-on: ubuntu-18.04
#     if: github.event.repository.owner.id == github.event.sender.id

#     steps:
#       - name: Checkout source 🛎️
#         uses: actions/checkout@master

#       - name: Setup Node.js
#         uses: actions/setup-node@v1
#         with:
#           node-version: "12"

#       - name: Install and Build 🔧
#         run: |
#           npm install
#           npm run build

#       - name: Deploy 🚀
#         uses: JamesIves/github-pages-deploy-action@4.1.1
#         with:
#           branch: dev-pages # The branch the action should deploy to.
#           folder: public # The folder the action should deploy.
#           ssh-key: ${{ secrets.BLOG_PRIVATE_KEY }}

# - name: setup hexo
#   env:
#     BLOG_PRIVATE_KEY: ${{secrets.BLOG_PRIVATE_KEY}}
#     BRANCH: dev-pages

# name: CI

# on:
#   push:
#     branches:
#       - develop

# env:
#   GIT_USER: QiongXing
#   GIT_EMAIL: qiongxing1412@qq.com
#   # THEME_REPO: sanonz/hexo-theme-concise
#   # THEME_BRANCH: master
#   DEPLOY_REPO: qiongxing/qiongxing.github.io
#   DEPLOY_BRANCH: dev-pages

# jobs:
#   build:
#     name: Build on node ${{ matrix.node_version }} and ${{ matrix.os }}
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         os: [ubuntu-latest]
#         node_version: [12.x]

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v2

#       - name: Checkout theme repo
#         uses: actions/checkout@v2
#         with:
#           repository: ${{ env.THEME_REPO }}
#           ref: ${{ env.THEME_BRANCH }}
#           path: themes/concise

#       - name: Checkout deploy repo
#         uses: actions/checkout@v2
#         with:
#           repository: ${{ env.DEPLOY_REPO }}
#           ref: ${{ env.DEPLOY_BRANCH }}
#           path: .deploy_git

#       - name: Use Node.js ${{ matrix.node_version }}
#         uses: actions/setup-node@v1
#         with:
#           node-version: ${{ matrix.node_version }}

#       - name: Configuration environment
#         env:
#           HEXO_DEPLOY_PRI: ${{secrets.BLOG_PRIVATE_KEY}}
#         run: |
#           sudo timedatectl set-timezone "Asia/Shanghai"
#           mkdir -p ~/.ssh/
#           echo "$HEXO_DEPLOY_PRI" > ~/.ssh/id_rsa
#           chmod 600 ~/.ssh/id_rsa
#           ssh-keyscan github.com >> ~/.ssh/known_hosts
#           git config --global user.name $GIT_USER
#           git config --global user.email $GIT_EMAIL
# # cp _config.fluid.yml themes/concise/_config.yml

#       - name: Install dependencies
#         run: |
#           npm install

#       - name: Deploy hexo
#         run: |
#           npm run build
#           npm run deploy

name: Hexo Deploy

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-18.04
    if: github.event.repository.owner.id == github.event.sender.id

    steps:
      - name: Checkout source
        uses: actions/checkout@v2
        with:
          ref: develop
          path: ./deploy_git

      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: "12"

      - name: Setup Hexo
        env:
          ACTION_DEPLOY_KEY: ${{ secrets.BLOG_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh/
          echo "$ACTION_DEPLOY_KEY" > ~/.ssh/id_rsa
          chmod 700 ~/.ssh
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts
          git config --global user.email "qiongxing1412@qq.com"
          git config --global user.name "QiongXing"
          npm install hexo-cli -g
          npm install

      - name: Deploy
        run: |
          hexo clean
          hexo deploy
